SELECT count(`naam`) AS aantal_kroegen
FROM `kroeg`
WHERE `kroegcode` in   (
	SELECT `kroegcode` 
	FROM `schenkt`
	WHERE `biercode` in   (
		SELECT `biercode`
		FROM `bier`
		WHERE `naam` = 'ZIPPIL'	
	)
)
