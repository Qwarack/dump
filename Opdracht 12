SELECT `naam`, `plaats` , `kroegcode`
FROM `kroeg`
WHERE `naam` LIKE 'DE*' AND `kroegcode` IN (
	SELECT `kroegcode`
	FROM `schenkt`
	WHERE `biercode` in (
		SELECT `biercode`
		FROM `bier`
		WHERE NOT `type` = 'SPECIAL' AND NOT`type` = 'ALE, BELGIA' 
	)
)
